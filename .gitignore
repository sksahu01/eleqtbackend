# Environment variables
# Keep sensitive information like database credentials and API keys out of version control.
.env
*.env
config.env
projectinfo.txt

# Dependencies
# The node_modules directory contains all of your project's dependencies.
# It can be very large and can be re-created by running `npm install`.
node_modules

# Lockfiles
# It's generally recommended to commit package-lock.json to ensure consistent
# dependency versions across all environments. However, if your team has a
# different workflow, you can ignore it.
# package-lock.json

# Logs
# Log files can contain sensitive information and grow very large.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output
# Compiled or built files that are generated from your source code.
dist
build
out

# IDE and editor configuration
# These files are specific to a developer's local setup.
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# OS-generated files
# System files that are not part of the project.
.DS_Store
Thumbs.db

# Test coverage reports
coverage